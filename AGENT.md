# 에이전트 프로젝트 지침 (Agent Project Instructions)

이 문서는 이 프로젝트에서 작업하는 모든 AI 에이전트가 준수해야 할 엄격한 규칙을 정의합니다. 모든 변경 사항을 커밋하거나 푸시하기 전에 이 지침을 반드시 확인하십시오.

## 1. 버전 관리 규칙 (Version Management)

- **선제적 버전 갱신**: 모든 코드 수정 및 기능 추가 후, 릴리즈 시점에 전체 플러그인 버전을 갱신해야 합니다.
- **단일 버전 모델 (Single Suite Versioning)**: 개별 스킬(`SKILL.md`)은 독자적인 버전을 가지지 않으며, 전체 플러그인 버전 하나로 통합 관리됩니다.
  - **전체 플러그인(Plugin)**: 배포 패키지 단위의 단일 버전을 따릅니다.
  - **개별 스킬(Skill)**: `SKILL.md`에서 `version` 필드를 제거합니다.
- **분리된 로그 체계 (Manual vs. Automated)**:
  - **`UPGRADE_HISTORY.md` (Manual Dev Log)**: `~/project/skills/docs/{skill_name}/` 폴더에 위치하며 (스킬 폴더 외부), 개발자가 직접 작성하는 문서입니다. 설계 고민, 아키텍처 결정 배경, 미해결 과제 등 "과정"을 자유롭게 기록합니다. 자동화 도구는 이 파일을 수정하지 않습니다.
  - **`CHANGELOG.md` (Automated Release Log)**: 프로젝트 루트에 위치하며, GitHub Action이 릴리즈 시점에 커밋 메시지를 분석하여 자동으로 생성/갱신합니다. 로컬 작업 시에는 존재하지 않을 수 있으며, 오직 자동화 봇에 의해서만 관리됩니다.
- **LLM 플러그인 버전 동기화**: 플러그인 배포를 위한 메니페스트 파일들은 항상 동일한 버전으로 동기화되어야 합니다.
  - **Claude (Claude Code)**: `/.claude-plugin/plugin.json`, `/.claude-plugin/marketplace.json`
  - **Cursor**: `/.cursor-plugin/plugin.json`
- **버전 Numbering 원칙**: 
  - **Major (x.0.0)**: 전체 아키텍처 변경, 하위 호환성 단절.
  - **Minor (0.x.0)**: 새로운 스킬 추가 또는 대규모 기능 개선.
  - **Patch (0.0.x)**: 버그 수정, 미세 조정, 특정 스킬의 마이너 업데이트.

## 2. 플랫폼 중립성 유지 (Platform Neutrality)

- **Universal Tool Calling**: `SKILL.md` 정의 시 특정 플랫폼에 종속되지 않도록 Gemini(`run_command`, `view_file` 등)와 Claude Code(`Bash`, `Read` 등)의 도구 이름을 `allowed-tools`에 모두 포함해야 합니다.
- **의도 기반 지침**: 스킬 내 가이드라인 작성 시 "Bash 도구를 사용하여" 대신 "시스템 명령 실행 도구를 사용하여"와 같이 플랫폼 독립적인 용어를 사용하십시오. 또한 프로젝트 내부 스크립트 실행 시 반드시 `$CLAUDE_PROJECT_DIR` 환경 변수를 경로 접두사로 사용하여 실행 환경 변동에 대비하십시오.

## 2. 작업 완료 및 제출 규칙 (Definition of Done)

커밋 및 푸시 전 반드시 아래 체크리스트를 확인하십시오:

1. **버전 정합성**: 
   - [ ] 배포용 메니페스트(`.claude-plugin/`, `.cursor-plugin/`)의 버전이 동일한가?
   - [ ] `marketplace.json`의 버전이 `plugin.json`과 일치하는가?
   - [ ] 수정된 스킬의 `SKILL.md` 버전이 해당 스킬의 히스토리에 맞춰 갱신되었는가?
2. **문서 동기화**:
   - [ ] 코드 변경 사항이 `README.md`, `ARCHITECTURE.md`, `UPGRADE_HISTORY.md`에 반영되었는가?
   - [ ] `~/project/skills/docs/{skill_name}/` 내의 문서가 최신 로직을 반영하고 있는가?
3. **커밋/푸시**:
   - [ ] 커밋 메시지가 명확하고 규칙을 따르는가?
   - [ ] `git push`가 성공적으로 수행되었는가?
- **내용 파악 및 보정**: 단순히 문서를 추가하는 것에 그치지 말고, 기존 문서의 내용을 깊이 있게 파악하여 현재의 수정 사항과 모순되는 부분이 있다면 반드시 수정하십시오.
- **변경 이력 기록**: 
  - 새로운 결정 사항이나 아키텍처 변경점은 반드시 **`UPGRADE_HISTORY.md`**에 상세히 기록하십시오.
  - 최종 사용자에게 공개될 변경 사항은 커밋 메시지를 통해 **`CHANGELOG.md`**에 자동 반영되도록 하십시오.

## 3. 커밋 및 푸시 가이드라인

- 모든 작업 완료 후에는 반드시 `git add`, `git commit`, `git push`를 수행하여 변경 사항을 원격 저장소에 반영하십시오.
- 커밋 메시지는 변경 사항을 명확히 설명해야 하며, 아래의 **커밋 메시지 규칙**을 엄격히 준수하십시오. 이는 자동 체인지로그 생성의 기반이 됩니다.

## 4. 커밋 메시지 및 체인지로그 규칙 (Conventional Commits)

자동화된 버전 관리와 스킬별 체인지로그 생성을 위해 다음 템플릿을 사용하십시오.

### 커밋 메시지 템플릿
```text
<type>(<scope>): <subject>

<body> (선택 사항)
- 구체적인 변경 내용 1
- 구체적인 변경 내용 2

<footer> (선택 사항: BREAKING CHANGE, 이슈 번호 등)
```

### Type 규정
- **feat**: 새로운 기능 추가
- **fix**: 버그 수정
- **chore**: 빌드 업무 수정, 패키지 매니저 설정, 문서 업데이트 (코드 변경 없음)
- **refactor**: 버그를 수정하거나 기능을 추가하지 않는 코드 변경
- **style**: 코드 의미에 영향을 주지 않는 변경사항 (화이트스페이스, 포맷팅 등)
- **test**: 테스트 추가 또는 수정
- **perf**: 성능 개선

### Scope 규정 (필수)
- 반드시 변경된 스킬 또는 컴포넌트명을 명시하십시오. (예: `e2e_test`, `refresh`, `ci`, `docs`)
- 여러 스킬에 걸친 변경일 경우 `all` 또는 대표 범위를 지정하십시오.

### 체인지로그 생성 방식
- GitHub Action이 태그 릴리즈 시, 이전 태그 이후부터의 커밋들을 분석합니다.
- 각 커밋 메시지를 분석하여 프로젝트 루트의 `CHANGELOG.md`를 자동으로 갱신합니다.
- **Breaking Change**: Footer에 `BREAKING CHANGE:`가 포함된 경우 Major 버전 상향 대상으로 간주됩니다.

## 5. 언어 및 응답 규칙

- 모든 설명과 답변은 **한국어**로 작성하십시오. (시스템 전역 규칙 준수)
- 건조하고 사실에 기반한 데이터 분석가 및 시스템 아키텍트의 관점을 유지하십시오.

## 5. 지시사항 분리 및 의도 확인 (Instruction Decoupling)

- **지시사항 전이 방지 (Anti-Instruction Bleeding)**: 이전 요청에서의 '수정/실행' 상태를 현재의 '파악/분석' 요청에 투영하지 마십시오. 새로운 프롬프트가 '파악, 분석, 조사, 리서치'를 요구할 경우, 자동 수정을 중단하고 오직 분석 보고서만 생성해야 합니다.
- **분석 vs 수정 명확화**: 사용자가 명시적으로 '수정해줘'라고 하지 않는 한, '파악해줘'라는 요청은 코드 변경을 수반하지 않는 'ReadOnly' 작업으로 간주하십시오.

---

> [!IMPORTANT]
> 이 규칙을 어기는 것은 프로젝트 컨벤션을 위반하는 것입니다. 항상 "분석 -> 수정 -> 버전 업 -> 문서 갱신 -> 검증 -> 커밋/푸시" 순서를 지키십시오.
